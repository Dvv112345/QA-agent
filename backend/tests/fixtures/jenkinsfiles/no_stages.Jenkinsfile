// A declarative pipeline with no `stages` block at all — legal enough to
// brace-balance, but nothing to splice a stage into.
pipeline {
    agent any
    options {
        timeout(time: 1, unit: 'HOURS')
    }
}
